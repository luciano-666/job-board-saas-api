[project]
name = "job-board-saas-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.136.3",
    "psycopg[binary]>=3.3.4",
    "sqlalchemy>=2.0.50",
]

[tool.fastapi]
entrypoint = "src.main:app"

[dependency-groups]
dev = [
    "ruff>=0.15.15",
]
